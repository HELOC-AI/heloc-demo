{
  "$schema": "https://backboard.railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm --filter @heloc/intake... --if-present run build",
    "watchPatterns": ["/apps/intake/**", "/packages/**", "/pnpm-lock.yaml", "/.node-version"]
  },
  "deploy": {
    "startCommand": "node apps/intake/src/main.ts",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
