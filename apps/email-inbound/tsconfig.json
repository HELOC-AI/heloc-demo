{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Runs on workerd (bundled by wrangler/esbuild), not Node.
    "lib": ["ES2023"],
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "types": ["@cloudflare/workers-types"]
  },
  "include": ["src"]
}
