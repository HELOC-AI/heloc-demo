{
  "$schema": "https://backboard.railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm --filter @heloc/figure-mock... --if-present run build",
    "watchPatterns": ["/apps/figure-mock/**", "/packages/**", "/pnpm-lock.yaml", "/.node-version"]
  },
  "deploy": {
    "startCommand": "node apps/figure-mock/src/main.ts",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
